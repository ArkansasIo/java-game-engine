#!/bin/sh
set -o errexit || exit $?

# Arklight.Utility relies on keeping the original working directory, so don't change directory
LAUNCHER=$(readlink -f "${0}")
HERE=$(dirname "${LAUNCHER}")
"${HERE}/../lib/Arklight/Arklight.Utility" {MODID} "$@"

